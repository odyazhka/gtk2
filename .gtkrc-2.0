style "colored_menu"
{
  bg[NORMAL] = "#f2f5ff"  # Тот самый нежно-голубой
  bg[PRELIGHT] = "#e8edff" # Цвет при наведении
}
widget_class "*Menu*" style "colored_menu"
widget_class "*MenuBar*" style "colored_menu"
